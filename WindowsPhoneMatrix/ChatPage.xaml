<Page
    x:Class="WindowsPhoneMatrix.ChatPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:WindowsPhoneMatrix"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    RequestedTheme="Dark">

    <Grid Background="#1E1E1E">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid Margin="20,20,20,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Button Content="&#xE72B;" FontFamily="Segoe MDL2 Assets" Background="Transparent" Foreground="White" FontSize="20" Click="BackButton_Click" Margin="0,0,15,0"/>

            <TextBlock Grid.Column="1" x:Name="RoomNameText" Text="Room Name" FontSize="24" FontWeight="Bold" Foreground="White" VerticalAlignment="Center"/>
        </Grid>

        <ListView Grid.Row="1" x:Name="MessagesListView" Margin="10,0,10,10" SelectionMode="None">
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                </Style>
            </ListView.ItemContainerStyle>

            <ListView.ItemTemplate>
                <DataTemplate>
                    <StackPanel HorizontalAlignment="{Binding MessageAlignment}" Margin="0,5,0,5" MaxWidth="300">
                        <TextBlock Text="{Binding Sender}" FontSize="12" Foreground="Gray" Margin="5,0,5,2" HorizontalAlignment="{Binding MessageAlignment}"/>
                        <Border Background="{Binding BubbleColor}" CornerRadius="8" Padding="12,8">
                            <TextBlock Text="{Binding Body}" FontSize="16" Foreground="White" TextWrapping="Wrap"/>
                        </Border>
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <Grid Grid.Row="2" Margin="10,10,10,20" Background="#2D2D30" Padding="5">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBox x:Name="MessageInputBox" PlaceholderText="Type a message..." AcceptsReturn="True" TextWrapping="Wrap" Background="Transparent" Foreground="White" BorderThickness="0" VerticalAlignment="Center"/>
            <Button x:Name="SendButton" Grid.Column="1" Content="Send" Click="SendButton_Click" Background="#0078D7" Foreground="White" Margin="10,0,0,0"/>
        </Grid>

        <TextBlock x:Name="StatusText" Grid.Row="2" Text="" Margin="0,0,0,-20" Foreground="Yellow" HorizontalAlignment="Center" VerticalAlignment="Bottom"/>
    </Grid>
</Page>
